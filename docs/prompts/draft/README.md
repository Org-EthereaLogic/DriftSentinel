# Prompt Drafts

Store work-in-progress prompts here while they are being shaped, tested, or reviewed.

Guidelines:

- Use clear, task-oriented filenames.
- Keep assumptions explicit while the prompt is still evolving.
- Move a prompt into `../enhanced/` only after validation confirms it is self-contained and execution-ready.

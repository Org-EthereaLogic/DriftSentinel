# Deployment dependencies for surfaces that install from requirements.txt.
# `-e .` ensures Databricks Apps installs this repository's local package.
-e .
gradio>=6.10.0,<7
plotly>=5.0,<6
pillow>=12.2.0

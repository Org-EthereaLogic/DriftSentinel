# tests

Product test suite for DriftSentinel.

| File | Purpose |
| --- | --- |
| `test_scaffold_layout.py` | Validates that all expected files and directories exist (92 tests) |
| `test_governance_guards.py` | Regression guards for banned scaffold markers and bundle invariants (2 tests) |

Run with `make test` or `uv run pytest`.

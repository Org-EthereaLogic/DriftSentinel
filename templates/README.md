# templates

YAML policy and contract templates for dataset configuration.

| File | Purpose |
| --- | --- |
| `dataset_contract.yml` | Schema and quality contract for a registered dataset |
| `drift_policy.yml` | Drift detection thresholds and gate rules |
| `benchmark_policy.yml` | Benchmark scenarios and pass/fail criteria |

Users copy and customize these templates for their own datasets.

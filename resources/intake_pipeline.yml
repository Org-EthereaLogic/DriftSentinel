resources:
  pipelines:
    intake_pipeline:
      name: "driftsentinel_intake"
      catalog: "${var.catalog}"
      target: "${var.schema}"
      serverless: true
      libraries:
        - notebook:
            path: ../notebooks/03_run_intake_controls.py
      configuration:
        catalog: "${var.catalog}"
        schema: "${var.schema}"

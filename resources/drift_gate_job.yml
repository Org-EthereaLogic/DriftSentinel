resources:
  jobs:
    drift_gate_job:
      name: "driftsentinel_drift_gate"
      tasks:
        - task_key: run_drift_gate
          notebook_task:
            notebook_path: ../notebooks/04_run_drift_gate.py
            base_parameters:
              catalog: "${var.catalog}"
              schema: "${var.schema}"

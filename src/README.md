# src/

Top-level source directory. Contains the `driftsentinel` first-party package.

All product code lives under `src/driftsentinel/` following the src-layout
convention. The package is installed in editable mode via `uv sync`.
